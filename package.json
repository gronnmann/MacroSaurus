{
    "name": "macrosaurus",
    "private": true,
    "packageManager": "pnpm@10.25.0",
    "scripts": {
        "prepare": "husky",
        "format": "pnpm --dir web format && mvn -pl backend spotless:apply",
        "format:check": "pnpm --dir web format:check && mvn -pl backend spotless:check",
        "quality": "pnpm --dir web quality && mvn -pl backend verify"
    },
    "devDependencies": {
        "husky": "^9.1.7"
    }
}
